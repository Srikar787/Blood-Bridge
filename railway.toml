# Railway configuration
# This helps Railway understand your Docker setup

[build]
  builder = "DOCKERFILE"
  dockerfilePath = "backend/Dockerfile"

[deploy]
  startCommand = "java -jar app.jar"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

